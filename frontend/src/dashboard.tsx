import { Card, CardContent, CardHeader } from "@mui/material";
    export const Dashboard = () => (
      <Card>
      <CardHeader title= "libby app" />
      <CardContent> An application for managing a list of books in a library < /CardContent>
      </Card>
    );